services:
  - type: web
    name: film-media-web-service
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    # 环境变量需要在Render控制台中手动配置
    # 不要在这里写入真实的API密钥
